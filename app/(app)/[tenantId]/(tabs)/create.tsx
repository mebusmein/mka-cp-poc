// This is a placeholder route for the create button in the tab bar.
// The actual create-modal is at the stack level and is opened via router.push.
// This file exists only to satisfy Expo Router's requirement for tab routes.

import { View } from 'react-native';

export default function CreatePlaceholder() {
  return <View />;
}

